sonata_admin:
    title: 'IDF Mobilités'
    title_logo: false

    options:
        confirm_exit: true
        use_icheck: false

    security:
        handler: sonata.admin.security.handler.noop

    dashboard:
        blocks:
            - { position: left, type: sonata.admin.block.admin_list }
